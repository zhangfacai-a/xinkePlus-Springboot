<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ruoyi.live.mapper.LiveCommentMapper">

    <insert id="insert" parameterType="com.ruoyi.live.domain.LiveComment" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO live_comment (room_id, user_id, content, create_time)
        VALUES (#{roomId}, #{userId}, #{content}, #{createTime})
    </insert>

    <select id="selectCommentList" resultType="com.ruoyi.live.mapper.vo.LiveCommentPageResp">
        SELECT id,
               user_id AS userId,
               content,
               create_time AS createTime
        FROM live_comment
        WHERE user_id = #{userId}
          <if test="content != null and content != ''">
              AND content LIKE CONCAT('%', #{content}, '%')
          </if>
          <if test="beginTime != null">
              AND create_time &gt;= #{beginTime}
          </if>
          <if test="endTime != null">
              AND create_time &lt;= #{endTime}
          </if>
        ORDER BY id DESC
    </select>

</mapper>
